apiVersion: v1
kind: Namespace
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: production-usw1:/Namespace:/production-usw1
  name: production-usw1
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: production-usw1:/Service:production-usw1/web-app-service
  labels:
    app.kubernetes.io/name: web-app
  name: web-app-service
  namespace: production-usw1
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: web-app
    app.kubernetes.io/name: web-app
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: production-usw1:apps/Deployment:production-usw1/web-app
  labels:
    app.kubernetes.io/name: web-app
  name: web-app
  namespace: production-usw1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
      app.kubernetes.io/name: web-app
  template:
    metadata:
      labels:
        app: web-app
        app.kubernetes.io/name: web-app
    spec:
      containers:
        - image: nginx:stable
          name: web-app
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: 250m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: production-usw1:networking.k8s.io/Ingress:production-usw1/web-app-ingress
    nginx.ingress.kubernetes.io/rewrite-target: /
  labels:
    app.kubernetes.io/name: web-app
  name: web-app-ingress
  namespace: production-usw1
spec:
  rules:
    - host: app.example.com
      http:
        paths:
          - backend:
              service:
                name: web-app-service
                port:
                  number: 80
            path: /
            pathType: Prefix
