apiVersion: v1
kind: Namespace
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: integration-staging:/Namespace:/intergration-staging
  name: intergration-staging
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: integration-staging:/Service:intergration-staging/web-app-service
  labels:
    app.kubernetes.io/name: web-app
  name: web-app-service
  namespace: intergration-staging
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: web-app
    app.kubernetes.io/name: web-app
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: integration-staging:apps/Deployment:intergration-staging/web-app
  labels:
    app.kubernetes.io/name: web-app
  name: web-app
  namespace: intergration-staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
      app.kubernetes.io/name: web-app
  template:
    metadata:
      labels:
        app: web-app
        app.kubernetes.io/name: web-app
    spec:
      containers:
        - image: nginx:stable
          name: web-app
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: 250m
              memory: 128Mi
            requests:
              cpu: 100m
              memory: 64Mi
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: integration-staging:networking.k8s.io/Ingress:intergration-staging/web-app-ingress
    nginx.ingress.kubernetes.io/rewrite-target: /
  labels:
    app.kubernetes.io/name: web-app
  name: web-app-ingress
  namespace: intergration-staging
spec:
  rules:
    - host: app.example.com
      http:
        paths:
          - backend:
              service:
                name: web-app-service
                port:
                  number: 80
            path: /
            pathType: Prefix
